基于边缘的模板匹配

这个模板匹配原理非常简单，MVTec具有详细的介绍，但是如何加速是关键，当前实现是基于金字塔+顶层SSE的方式，但是小图达不到商业库的速度
优化方向：离散方向表+SSE查表的方式，可能还可以融合霍夫变换的模板匹配算法

这个算法虽然看起来只是一个算法，但是包含了：亚像素边缘提取、图像旋转、金字塔和非极大值抑制等很多部件，所以这也是为什么商业库这么快，因为是一个综合因素，
单一的优化对此算法都有制约的作用，所以说这个算法需要具有一定的算法沉淀。
